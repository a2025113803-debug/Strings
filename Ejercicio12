#include <iostream>
#include <string>
using namespace std;

string BorraCaracter(string texto, char c) {
    string r = "";
    for (char x : texto)
        if (x != c) r += x;
    return r;
}

int main() {
    cout << BorraCaracter("hola mundo", 'o');
}

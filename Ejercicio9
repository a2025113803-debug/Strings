#include <iostream>
#include <string>
using namespace std;

int main() {
    string A, B;
    getline(cin, A);
    getline(cin, B);

    string C = "";
    for (char c : A)
        if (B.find(c) != string::npos && C.find(c) == string::npos)
            C += c;

    cout << "C = " << C;
}

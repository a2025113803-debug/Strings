#include <iostream>
#include <string>
using namespace std;

int BuscarSubCadena(string sub, string cad) {
    int pos = cad.find(sub);
    return (pos == string::npos ? -1 : pos);
}

int main() {
    cout << BuscarSubCadena("oool", "hooola");
}

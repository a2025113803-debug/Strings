#include <iostream>
#include <string>
using namespace std;

int main() {
    string cad;
    cin >> cad;

    string invertida = string(cad.rbegin(), cad.rend());

    cout << invertida << endl;
    if (invertida == cad) cout << "Es capicua";
    else cout << "No es capicua";
}
